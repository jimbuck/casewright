# Use a workspace-local store so CI caching is predictable.
# NW.js ships a large prebuilt binary; allow its postinstall to run (see
# pnpm.onlyBuiltDependencies in the root package.json).
auto-install-peers=true
